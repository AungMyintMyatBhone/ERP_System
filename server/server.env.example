# MongoDB Atlas Connection String
# Replace with your actual MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Database Name
DB_NAME=erp_system

# Server Configuration
PORT=5001
NODE_ENV=development

# JWT Secret for Authentication
# Generate a secure random string for production
JWT_SECRET=your_jwt_secret_key_here_please_change_in_production

# CORS Configuration
CORS_ORIGIN=http://localhost:3001

# Instructions:
# 1. Copy this file to server/server.env
# 2. Replace MONGODB_URI with your actual MongoDB Atlas connection string
# 3. Generate a secure JWT_SECRET for production use
# 4. Update other configuration values as needed